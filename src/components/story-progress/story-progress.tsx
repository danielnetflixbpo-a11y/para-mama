"use client";
import { useEffect, useState } from "react";
import { content } from "@/data/content";
import styles from "./story-progress.module.css";
export function StoryProgress(){const [active,setActive]=useState(0);const [visited,setVisited]=useState([0]);useEffect(()=>{const nodes=content.story.chapters.map((chapter)=>document.getElementById(chapter.id)).filter((node):node is HTMLElement=>Boolean(node));const observer=new IntersectionObserver((entries)=>entries.forEach((entry)=>{if(entry.isIntersecting){const index=nodes.indexOf(entry.target as HTMLElement);if(index>=0){setActive(index);setVisited((current)=>current.includes(index)?current:[...current,index]);}}}),{threshold:.45});nodes.forEach((node)=>observer.observe(node));return()=>observer.disconnect();},[]);return <nav className={styles.progress} aria-label="Capítulos de esta historia"><span>{content.story.chapters[active]?.label}</span><div>{content.story.chapters.map((chapter,index)=><button key={chapter.id} type="button" disabled={!visited.includes(index)} aria-current={active===index?"step":undefined} aria-label={chapter.label} onClick={()=>document.querySelector(chapter.target)?.scrollIntoView({behavior:"smooth"})}/>)}</div></nav>}
